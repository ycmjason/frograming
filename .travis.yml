language: node_js

node_js: 8

env:
  - NODE_ENV=ci

install:
  - npm ci
  - npm run bootstrap

script:
  - npm test
  - npm run build

deploy:
  skip_cleanup: true
  - provider: script
    script: npm run deploy:staging
    on:
      branch: master
  - provider: script
    script: npm run deploy:production
    on:
      tags: true
      branch: master
